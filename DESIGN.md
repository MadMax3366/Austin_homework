# 学生管理系统设计

## 1. 业务理解

这不是“学生资料 CRUD”，而是教育机构的运营履约系统：它需要把咨询、试听、报名购课、固定班、实际课次、出勤反馈、续费或流失串成可追踪的生命周期，并把原先依赖 Excel、微信群和员工记忆的规则变成系统约束。

主要角色：

- **Admin／课程顾问／教务**：负责线索、学生归属、试听、分班、课时和续费。
- **Teacher**：关心今天上什么课、名单里有谁、谁是新生，并完成点名和课堂反馈。
- **Manager**：处理人员转交、课时调整、退款及异常审计；首版不做独立界面。
- **Student 与 Guardian**：必须分开建模；一个家长可关联多个孩子，一个学生也可有多个联系人。

系统最重要的不是页面数量，而是业务事实一致、可追责、可更正。例如固定班不是某天实际发生的课；课时接近钱，余额不能只是一个随意覆盖的数字。

## 2. 本版范围与非目标

本版实现一条完整切片：

> 老师登录 → 查看 Melbourne 当天由自己授课的课程 → 查看有效名单及课时预警 → 整班点名 → 服务端原子写入出勤与课时流水 → 保存课堂记录和可编辑的 AI 家长反馈草稿。

本版故意不做咨询／试听、排班编辑、购课支付、退款、续费消息、补课、家长端、报表、多校区和完整后台管理。这些能力需要在整体模型中留出演进空间，但不应挤压本次切片的权限、一致性和失败处理。

## 3. 数据模型

完整领域模型：

```text
Student ─< StudentGuardian >─ Guardian
   │
   ├─< AdminAssignment >─ Admin       （有起止时间，保留转交历史）
   ├─< Inquiry / Trial / FollowUp
   ├─< Enrollment >─ ClassSeries ─< LessonSession >─ Teacher
   │                                  │
   │                                  └─< Attendance
   └─ CreditAccount ─< CreditTransaction
```

- `ClassSeries` 表示每周固定班、默认老师、容量和本地时间规则；`LessonSession` 是某个具体日期的实际课，可单独取消、改时或代课。
- `Enrollment` 是学生与班级的带生效区间多对多关系，支持转班及同时参加多个班。
- `Attendance(session_id, student_id)` 唯一，记录实际发生的出勤事实。
- `CreditTransaction` 是不可变账本：购课 `+n`、出勤 `-1`、人工调整 `±n`、更正用反向流水；余额由流水求和，不直接修改。
- 目标模型使用 `AdminAssignment` 保存负责人历史。本切片只读取 `Student.owner_admin_id`，因为负责人转移不属于本次流程。

实际代码只落地该切片需要的表，同时保留 `Guardian`、学生负责人和审计表，避免切片模型与未来全局模型冲突。

## 4. 关键规则与执行层

| 规则 | 执行位置 |
|---|---|
| 老师只能读取和修改分配给自己的具体课次；客户端提交的角色、teacherId 不可信 | 服务端认证与对象级授权；数据库触发器再校验课次老师 |
| 名单按课次日期落在 Enrollment 生效区间内计算；非名单学生不能点名 | 服务查询 + 数据库触发器 |
| 同一学生在同一课次最多一条 Attendance | 数据库唯一索引 |
| 仅允许 `present / late / absent` | 服务端 Zod + 数据库 CHECK |
| 本版 `present / late` 扣 1 课时，`absent` 不扣 | 服务端计费策略 |
| 出勤与课时流水在同一个 D1 batch 事务中写入 | 服务层事务 |
| 余额不足不能抹掉已发生的出勤：保存 Attendance 为 `pending_insufficient_credit`，不写负数流水，交 Admin 处理 | 服务层条件写入 + 数据库防透支触发器 |
| 一次出勤最多产生一笔扣课流水 | `(account, kind, source_type, source_id)` 数据库唯一索引 |
| 客户端重试同一个 Idempotency-Key 返回原结果；相同 key 携带不同数据返回 409 | 服务层 + Session 中保存 key/hash |
| 已完成课次对老师只读；更正应追加 reversal，不能更新／删除原流水 | 服务层状态机 + 账本不可变触发器 |
| 未来、取消、其他老师的课不能完成 | 服务层，以 `Australia/Melbourne` 判断 |
| AI 只能整理文字，不能决定权限、出勤或扣课；无 key、超时或非法结构时使用确定性降级 | 独立 AI 服务 + JSON Schema + Zod；不进入出勤事务 |

## 5. LLM 设计

老师输入课堂速记后，可生成以下严格结构：

```ts
{
  summary: string
  strengths: string[]
  nextSteps: string[]
  guardianMessageDraft: string
}
```

调用只发生在服务端；明显的邮箱和电话先被移除；输出受 JSON Schema、长度和数组数量限制并再次经 Zod 校验。草稿始终可编辑，只有完成课次时才随记录保存，本切片不会自动发给家长。没有 API key、provider 超时或输出畸形时，系统返回保留老师原文的本地结构化草稿，点名流程照常工作。

## 6. 假设与最想确认的问题

当前假设：

- 所有业务输入、展示和“今天”均按 `Australia/Melbourne`；周期班保存当地星期／时间，具体课次保存当地日期／时间和 IANA zone。
- 每次正常正式课消耗一个课时；迟到同样扣课，缺席不扣。
- 新生在本切片中指尚无更早有效出勤记录的学生。
- 老师能看到姓名、学习上下文和课时预警，但看不到付款明细及完整家长联系方式。
- 已完成点名需由 Admin 通过反向流水纠错。

最希望业务方回答：

1. 请假、无故缺席、迟到、补课分别是否扣课？
2. 余额不足时允许欠课、阻止上课，还是进入待处理队列？
3. 课时属于学生、家庭、课程还是具体课包；退款、赠课和到期如何处理？
4. 老师可以在多久内自行修改点名，何时需要 Admin 审批？
5. 反馈最终发给谁、走什么渠道、发送前是否需要 Admin 审核？

## 7. 页面与信息结构

老师只有两个界面层级：

```text
登录
  └─ Today / 课程工作台
      ├─ 左侧：今天由我授课的课次（完成状态、时间、人数）
      └─ 主区：当前课次
          ├─ 名单、新生、余额预警
          ├─ 每人 Present / Late / Absent
          ├─ 课堂速记 + AI 可编辑草稿
          └─ 完成课次（显示将扣课数与待处理数）
```

桌面为侧栏加工作区；手机端课程切换变成顶部横向按钮，学生行变卡片，主要操作固定在底部。页面实现 loading、无课、加载错误、保存冲突、AI 降级和完成只读状态；所有状态都有文字，不只依赖颜色。

## 8. 选择这个切片的理由

它同时命中“老师不知道今天谁是新来的”和“课时用完才发现”两个痛点，并能在有限时间内证明班级／课次分离、角色权限、有效名单、不可变课时账本、事务、幂等、余额异常以及 AI 降级。相比做五个半成品 CRUD 页面，这条流程更窄，但可直接演示并接受绕过 UI 的破坏测试。

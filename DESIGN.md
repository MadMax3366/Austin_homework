# 学生运营系统设计（Part A）

## 1. 业务理解

系统覆盖学生从咨询到续费／流失的完整生命周期：咨询 → 试听 → 报名购课 → 入班排课 → 上课履约 → 课时消费 → 续费、退费、暂停或流失。

| 角色 | 首要关注 | 高频操作 |
|---|---|---|
| 运营 | 今日待办、学生状态、试听转化、排课、低课时、老师请假 | 跟进、排试听、转正式、加班／退班、续费／退款、转负责人、安排代课 |
| 教师 | 本周课表、学生名单、课堂履约 | 点名、课堂反馈、完成课次、提交请假 |
| 主管 | 全局负载、冲突、退款、薪资和例外 | 转移学生、审批退款／薪资／请假、查看审计 |
| 学生 | 本人课表、出勤、反馈、课时与订单 | 查看课程、续费、支付、提交问题 |
| 家长 | 关联孩子的课表、反馈、课时与订单 | 切换孩子、续费、支付、提交问题 |
| 系统管理员 | 集成、任务、审计与故障 | 健康检查、任务处理、申请限时支持 |

## 2. 第一版解决与故意不解决

第一步先理清与业务挂钩、可继续扩展的五层架构，再实现端到端基础框架：

1. 建立独立账户、角色工作台、对象范围授权和审计链路。
2. 打通咨询、试听、正式入班、统一排课、点名、课时、续费和退款的基本闭环。
3. 提供教师、运营、主管、学生、家长和系统管理员的基础工作台。
4. 接入 LLM，用于教师反馈草稿与批准 FAQ 分流；失败不阻断核心业务。

重要但第一版不解决：完整工资单、银行代发，以及真实 Email／SMS／WeChat 渠道。当前仅保留薪资累计、消息、集成配置和 outbox 边界，方便以后替换真实服务。

## 3. 数据模型

| 实体 | 关键字段与关系 |
|---|---|
| Organization / UserAccount / AccountRoleAssignment | 机构状态；登录账号；账号到教师、运营、主管、学生、家长或系统管理员的角色与数据范围 |
| AccountCredential / AccountSession | PBKDF2 密码摘要、失败锁定；服务端会话摘要、过期与撤销 |
| StaffUser / TeacherLeaveRequest / TeacherSubstitution | 员工类型与启停；老师请假日期和审批状态；受影响课次的原老师、代课老师与安排人 |
| Student / Guardian / StudentGuardian | 学生状态、负责人；家长联系信息；多对多亲属关系与主要联系人 |
| Inquiry / TrialBooking / FollowUpTask | 咨询来源与招生状态；试听课次、出席结果和转化决定；负责人待办 |
| ClassSeries / LessonSession | 固定班的星期、时间、老师、教室和容量；一次真实课次的日期、老师、类型和状态 |
| Enrollment / SessionParticipant / Attendance | 学生入班关系；课次冻结名单；出勤、迟到或缺席及课时处理结果 |
| CreditAccount / CreditTransaction / BillingException | 学生课时账户；购买、消费、调整和冲正流水；余额不足或异常处理 |
| Order / PaymentTransaction / Refund | 课包订单；支付事件；退款申请、审批、付款与课时冲销 |
| TeacherPayRate / PayrollPeriod / PayrollEntry | 老师按课次类型和生效日期的费率；薪资周期；每个已履约课次的薪资记录 |
| Message / FAQInteraction / OutboxEvent / AuditEvent | 站内消息与人工接管；FAQ 决策；待发送事件；不可变操作审计 |

## 4. 必须由系统强制的规则

“时间区间不得重叠”使用半开区间 `[start, end)`：当 `A.start < B.end` 且 `B.start < A.end` 时冲突；`A.end == B.start` 表示前一节刚结束、后一节开始，允许相邻排课。

| 规则 | 架构层与功能模块 | 强制方式 |
|---|---|---|
| 必须登录；停用账号／机构不可访问；每个账号只进入获授权工作台 | ② 身份与访问控制层／账户、会话、角色授权 | PBKDF2、HttpOnly 会话、服务端 RBAC、对象范围查询 |
| 运营可查看全机构学生，但只能修改本人负责学生；主管可转移负责人 | ② 身份与访问控制层／数据范围；④ 学生管理 | 每次写操作重新校验 owner；转移同时更新开放咨询和待办 |
| 老师、教室、学生的真实课次不得时间重叠；相邻课次允许 | ④ 班级与排课；⑤ 关系数据库 | 服务层预检 + D1 trigger 防并发穿透 |
| 同一学生可参加多个班，但相同星期和时间不能重叠；班级不能超容量 | ④ 班级与排课 | 加班状态机、容量查询、课次名单 trigger |
| 试听与正式课使用同一排课／点名模型，但试听不消费正式课包 | ④ 招生试听、教学履约、课时财务 | `session_kind`、participant 来源和计费策略 |
| 试听 cancelled／no-show 不能作为“试听转化成功”的依据；未试听直接报名走独立的人工入班路径 | ④ 招生试听、学生管理 | 转化状态机与显式 `enroll_student` 命令分离 |
| 正式课 Present／Late／Absent 均扣 1 课时；获批豁免不扣；1 课时对应 1 小时 | ④ 教学履约、课时财务 | 服务端计费策略；账本只追加，不直接改余额 |
| 试听免费，但老师按正常课时计薪；补课／一对一可使用独立课次类型和费率 | ④ 教学履约、老师薪资 | 课次类型快照 + 生效日期费率 |
| 余额不足仍保存真实出勤，但不能形成负余额 | ④ 教学履约、课时财务；⑤ 数据库 | 同一事务创建 BillingException；数据库阻止透支 |
| 支付、完成课次、账本来源只能成功一次；相关记录全成或全回滚 | ③ 应用服务层；⑤ 数据库 | Idempotency-Key、唯一业务键、D1 batch transaction |
| 老师请假批准后才能安排代课；代课老师也必须通过时间冲突校验 | ④ 教师调度、班级与排课 | 请假／代课状态机 + 老师冲突 trigger |
| 退款申请人与审批人分离；只冲销尚未消费的课时 | ④ 课时财务；⑤ 数据库 | 主管审批、余额检查、退款流水与 reversal 原子写入 |
| LLM 只能生成文本草稿或选择批准 FAQ；非法结构、超时和无 Key 必须降级 | ③ 应用服务层／智能文本辅助 | Structured Output、Zod 校验、allowlist、本地 fallback／人工接管 |

## 5. 假设与最想确认的问题

当前假设：1 课时兑换 1 小时；试听对学生免费，老师按正常时薪计费；正式课缺席仍扣课，获批请假／豁免不扣；当前为单机构系统。

最想向业务方确认：

1. 机构只有班级课，还是同时存在 1:1、半私教和临时小组课？不同类型如何定价和计薪？
2. 学生迟到、缺席、请假、补课分别在什么条件下扣课、返还或生成特殊课时？
3. 学生转给另一位运营时，历史跟进归属、未完成任务和绩效如何迁移？
4. 系统是否需要内置老师、运营、学生和家长的交流渠道，还是继续使用外部微信群，仅在系统记录结果？

## 6. 页面与信息结构

| 工作台 | 主要入口 |
|---|---|
| 运营 | 今日待办、学生管理、排课中心、招生试听、课时财务、教师调度 |
| 教师 | 本周课表、课堂点名、课堂反馈、请假申请、个人薪资 |
| 主管 | 运营负载、冲突、退款、薪资、学生转移、支持审批 |
| 学生 | 本人周／日课表、出勤反馈、剩余课时、续费订单、帮助中心 |
| 家长 | 关联孩子切换、孩子课表、反馈、课时、订单、帮助中心 |
| 系统管理 | 集成健康、任务、outbox、审计和临时支持 |

## 7. Part B 选择理由

Part B 选择“运营处理一个学生从咨询试听到正式入班，再到低课时续费／退款”的垂直切片。它直接覆盖题面中的试听后无人跟进、排课冲突和课时不足三类事故，同时能在一条流程中展示：

- 独立账户登录、角色与 owner 权限；
- 家长与学生分离、试听与正式课分离；
- 班级、课次、名单、出勤和课时账本的关系；
- 可执行的排试听、记录结果、转正式、加班／退班、续费、退款和异常处理；
- 老师点名、反馈、请假与代课对运营链路的影响；
- LLM Structured Output、服务端校验和失败降级。

这个切片既能按正常路径演示，也能主动测试跨 owner、重复提交、两个班时间冲突、余额不足、支付重放、已消费课时退款和代课冲突等边界情况。

# Optional server-only Gemini configuration. Never commit a real key.
# Without it, teacher feedback uses a validated local fallback and FAQ triage
# uses approved deterministic matches or human handoff.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite

# 面试官分析与评分指南

## 1. 这道题真正考什么

附件是作业说明，不是候选人提交。它表面要求学生管理系统，实际在考：

1. 能否从模糊业务中识别角色、事实、状态与风险，而不是复述题面。
2. 能否在固定时间内主动缩小范围，并解释为什么不做某些功能。
3. 是否理解有时间属性的关系：班级不等于课次、当前负责人不等于负责人历史、余额不等于课时流水。
4. 是否把权限、状态机、扣课、幂等和并发规则放在可信的服务端／数据库边界。
5. 是否交付一条从数据库到 UI 真正闭环的流程，而不是多个静态 CRUD 页。
6. 是否知道确定性代码和 LLM 的边界。
7. 候选人是否能解释每个关键取舍和 AI 生成的代码。

机构规模约 1,000 名学生、20 名老师、每周 60 节课，模块化单体、关系库和少量高价值测试足够。微服务、消息总线、CQRS 或复杂权限 DSL 通常是过度设计。

优秀候选人应主动识别：Guardian 与 Student 分离；试听和正式课不同；固定班与某天课次不同；Admin owner 需要转交历史；课时有资金属性；“试听只能一次”只是规则示例；Melbourne 固定显示仍涉及 DST 和周期课程语义。

## 2. 建议的 100 分 Rubric

每项按 0–4 档评分：0 缺失、1 弱、2 部分达标、3 扎实、4 优秀；该项得分为 `权重 × 档位 ÷ 4`。

| 维度 | 权重 | 优秀表现 |
|---|---:|---|
| 业务理解与假设 | 18 | 识别家庭联系人、试听／正式课、课时资金属性、角色目标和关键歧义；问题按业务风险排序 |
| 范围与优先级 | 12 | 一条高价值闭环，非目标明确，每项取舍与用户痛点、风险和时间预算相关 |
| 数据模型 | 15 | 区分班级／课次、报名／出勤、当前状态／历史、余额／流水，并可局部演进 |
| 服务端规则、权限与一致性 | 18 | 对象级授权、数据库约束、事务、幂等与并发假设清楚，并有负向测试 |
| 垂直切片完整性 | 15 | 真实 DB 到 UI 的完整读写，seed 可信，边界和失败后恢复明确 |
| 代码、测试与可解释性 | 10 | 领域命名清楚，高价值测试，README 可复现；能说明替代方案及代价 |
| 信息架构与 UI 状态 | 7 | 角色首页即工作入口，操作短，loading／empty／error／冲突易理解 |
| LLM 使用 | 5 | 真正省人工，结构化输出、服务端校验、人审、隐私边界和失败降级 |

建议区间：

- **85–100**：非常强；
- **70–84**：扎实达标；
- **55–69**：证据混合，面试重点验证；
- **55 以下**：核心能力证据不足。

完成度上限建议：缺 DESIGN 或代码之一最高约 35；无真实数据库／后端／登录最高约 50；核心规则仅前端或明显横向越权最高约 60；按 README 无法跑通主流程最高约 55（临时环境问题除外）。LLM 不可用但降级正常，不应限制主流程得分。泄露 key 或真实学生数据应单独按严重安全事件处理。

## 3. 现场破坏测试

### 权限

- 未登录直接请求名单和提交点名，应返回 401 且零写入。
- Teacher A 猜到 Teacher B 的 `sessionId` 并直接 POST，应返回 403。
- 在 payload 中伪造 `teacherId`、role、余额或扣课数量，服务端必须忽略／拒绝。
- 查询响应不应泄露家长联系方式、付款详情或其他老师的学生。

### 事务与幂等

- 双击或并发提交同一 Idempotency-Key，只能出现一条 Attendance 和一笔扣课流水。
- 同一个 key 修改 payload，应返回 409，原结果不变。
- 在写 Attendance 后模拟 ledger 失败，不能留下半成品。
- 绕过 HTTP 直接重复来源，数据库唯一约束仍应阻止二次扣课。
- 已完成出勤不得直接改写或删除原流水；纠错应追加 reversal。

### 业务边界

- 余额 1 的到课成功扣至 0；余额 0 时不能形成负账本。候选人可以选择整批拒绝或保存出勤并进入异常队列，但必须明确、一致。
- 非当前 Enrollment 的学生不得塞进名单；整班提交不能漏人或重复学生。
- 已取消、未来、其他老师或已完成课次不能被普通老师再次完成。
- 相邻时段与真正重叠的半开区间 `[start, end)` 应有一致政策。
- 老师临时请假／代课应修改具体 Session，不应篡改整个固定班。

### LLM

- 无 key、超时、429、5xx、非 JSON、缺字段、超长输出都应进入安全 fallback。
- AI 失败不能回滚出勤或课时。
- Prompt 中含注入文本或恶意 HTML，输出仍需过固定 Schema，并由 UI 转义。
- 不得向模型发送无关电话、邮箱、完整生日；AI 不能自动改出勤、扣课或发家长消息。

### 追问

- “一个学生可由多个 admin 协作，要改哪几层？”
- “课时只能用于特定课程，账本如何演进？”
- “为什么某条规则放数据库、服务层或 UI？”
- “SQLite 换 Postgres 后哪些并发假设要重审？”
- “指出一个 AI 建议但你没采用的设计，并说明原因。”

## 4. 题面公平性风险

- 设计、全栈、认证、权限、seed、状态、LLM 和部署全部要求，6–10 小时偏紧。
- “首次到末次 commit 不超过 10 小时”与“休息自定”矛盾，且容易人为调整。
- public repo 会暴露候选人信息，也让历届答案可搜索；更适合 private repo 邀请评审。
- Markdown“三页”不可稳定定义，建议改成约 1,500 字、图表不计。
- 强制自备 LLM key 会受地区、支付方式和 provider 可用性影响；应提供测试 key，或允许 mock／fallback。
- “Australia/Melbourne，不做时区转换”没有说明存储与 DST，建议改为“输入输出固定 Melbourne，存储策略由候选人说明”。
- React、部署、MCP／Skills 不应成为未公开的隐藏分。

评审应接受任何明确、内部一致并在服务端执行的合理假设，不按面试官脑中的隐藏答案评分；部署成功和视觉精美度不单独加分。先由评审人独立记录证据和分数，再讨论，可减少光环效应。

## 5. 如何使用本参考实现

本目录实现“老师点名 + 课时账本 + AI 反馈”这一种合理答案，不是标准答案或最低功能清单。候选人选择“试听后跟进”等其他高价值闭环，同样可满分。不要要求其复制页面、表名、技术栈或业务政策；应根据上面的 Rubric 和候选人自己的 DESIGN 承诺评分。

本参考实现也有刻意边界：单组织、两个核心角色、合成数据、D1／SQLite、平台登录、无支付／通知／家长端／完整 Admin UI。高级候选人应能说明这些边界在生产中如何演进，但无需在限时作业里全部实现。
